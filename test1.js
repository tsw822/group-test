#include "stdio.h"
#define PI 3.14
in("t main()
{
    float radius;
    float high;
    float volume;
    scanf("%f", &radius);
    scanf("%f", &hight);

    volume = PI * radius * radius * high;

    printf("%.2f", volume);
    return 0;
}